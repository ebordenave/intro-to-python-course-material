# This module accepts user input from the console and then prints the reverse of the user input to the console.
# This script should not print any other text to the console other than the reverse user input. Do not print
# any input prompt.

var = input()
print(var[::-1])
